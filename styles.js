import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: "#fff",
      alignItems: "center",
      justifyContent: "center",
    },
    example: {
      margin: 16,
      padding: 16,
      borderWidth: 2,
      borderColor: "yellow",
    },
  });